---
  - name: loop over server deploys
    hosts: localhost
    tasks:
      - include_role:
          name: deploy_os_servers.yaml
          vars_from: osp_server_values.yaml
        with_dict: "{{ osp_servers }}"
      - debug:
          msg: successfully deployed { item.value.name }
...
